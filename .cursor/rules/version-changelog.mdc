---
description: 每次功能/修复完成后更新版本迭代说明
alwaysApply: true
---

# 版本迭代说明（必做）

完成**有用户价值的代码或文档改动**后，同步更新 `docs/版本迭代说明.md`：

1. 在「版本总览」表增加或更新对应版本行（如 v0.5.1、补丁说明）。
2. 在对应版本章节写清：**交付内容**、**涉及模块**（api/web/admin/shared/docs）、**升级注意**（命令、迁移、Breaking）。
3. 若变更接口/枚举/状态机，同时按 `docs/README.md`「变更同步规则」更新《共享接口与约定》等文档。

**可跳过**：纯问答、仅阅读代码、用户明确说「不要改文档」、或改动极小且无行为变化（如 typo 注释）。

**不要**为每次改动单独新建文档；只维护 `docs/版本迭代说明.md` 这一处发版记录。
