---
description: Web 商城样式改动须做断点适配（禁止 min/max/clamp）
globs: "apps/web/**/*.{vue,css,scss,ts}"
alwaysApply: false
---

# Web 响应式断点（必做）

适用范围：`apps/web`（Nuxt 商城）。**每次新增或修改样式**（布局、间距、字号、栅格、显隐）时，先判断是否在下列断点需要不同表现，并一并改完。

## 断点列表（桌面优先）

| 断点 | 含义 | 写法 |
|------|------|------|
| **1920px** | 默认基准，大屏样式 | 不写 media，作为默认样式 |
| 1440px | 较窄桌面 | `@media (max-width: 1440px)` 或 Tailwind `bp-1440:` |
| 1366px | 常见笔记本 | `bp-1366:` |
| 1280px | 小屏笔记本 | `bp-1280:` |
| 1024px | 平板横屏 | `bp-1024:` |
| 768px | 平板竖屏 | `bp-768:` |
| 480px | 大手机 | `bp-480:` |
| 375px | 小手机 | `bp-375:` |

桌面优先：默认按 1920 设计，**向下**用 `max-width` 覆盖，勿混用未约定的 `sm`/`md` 等 Tailwind 默认断点。

## 实现方式

1. **Tailwind（首选，见 `web-tailwind.mdc`）**：仅用 `apps/web/tailwind.config.ts` 中定义的 `bp-*` 前缀；非必要不新增 CSS 文件、不写大块 `<style>`。
2. **不得已的 CSS**：只用上表 `max-width` 媒体查询，按 1440 → 375 从大写到小，并注释为何不用 Tailwind。
3. **布局类改动**：栅格列数、横向滚动、顶栏折叠、字号间距等，至少在 **1024 / 768 / 375** 三档检查是否合理。

## 禁止

- **禁止**在 CSS 中使用 `min()`、`max()`、`clamp()` 函数（含 `width: min(…)`、`padding: clamp(…)` 等）。
- 需要弹性尺寸时用：百分比、`flex`、`grid`、固定多档 media、或 Tailwind 间距/宽度类，**不要**用上述函数折中。
- 禁止引入 Tailwind 默认 `sm:`/`md:`/`lg:` 等未在 `tailwind.config.ts` 登记的断点类。

## 自检（改动后）

- [ ] 样式以 Tailwind `class` 为主，未新增非必要 CSS 文件
- [ ] 默认（1920）样式已写
- [ ] 窄屏关键断点（至少 1024、768、375）已覆盖或说明无需变
- [ ] 未使用 `min()` / `max()` / `clamp()`
- [ ] 未使用未约定的 Tailwind 响应式前缀
