---
description: Harness 工程化核心约束（约束、上下文、验证闭环）
alwaysApply: true
---

# Harness 核心

## 三层结构

1. **约束**：`.cursor/rules/`、`AGENTS.md`、`docs/共享接口与约定.md`
2. **上下文**：`docs/README.md`、`harness/progress.md`、`specs/<name>/`
3. **验证**：`./harness/verify.sh`、交付前自检

## 任务路由

| 场景 | 动作 |
| ---- | ---- |
| 小修复、单文件、需求已明确 | 直接实现 → `verify.sh types` |
| 跨模块、新功能、验收模糊 | 先读 `.agents/skills/simplemall/references/spec-workflow/SKILL.md`，产出 `specs/` |
| TCB 部署 | 读 `.agents/skills/simplemall/references/deploy-tcb/SKILL.md` + `docs/TCB部署说明.md` |
| CloudBase 平台能力 | 读 `.agents/skills/cloudbase/SKILL.md` |

## 交付前必做

1. 运行 `./harness/verify.sh types`（触及构建/部署则 `all`）
2. 有用户价值 → 更新 `docs/版本迭代说明.md`
3. 改接口/枚举 → 同步 `docs/共享接口与约定.md`
4. 更新 `harness/progress.md` 的「最近完成」与「下一步」

## 禁止

- 未要求时 `git commit` / `git push`
- 跳过验证声称「已完成」
- 在 `packages/shared` 之外硬编码订单/SPU 等枚举
