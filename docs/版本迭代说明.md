# SimpleMall 版本迭代说明

本文档记录 Monorepo 各阶段的交付范围、关键变更与升级注意点。与代码版本号（当前根目录 `package.json` 为 **0.1.0**）及架构/接口文档修订记录互补。

---

## 版本总览

| 版本    | 日期           | 主题                                                                                     | 状态     |
| ------- | -------------- | ---------------------------------------------------------------------------------------- | -------- |
| v0.1.0  | 2026-06-03     | 架构与设计文档体系                                                                       | 已完成   |
| v0.2.0  | 2026-06-03     | Monorepo 骨架与开发脚本                                                                  | 已完成   |
| v0.3.0  | 2026-06-03     | 三端核心业务闭环                                                                         | 已完成   |
| v0.4.0  | 2026-06-03     | 演示数据、工程修复与后台体验                                                             | 已完成   |
| v0.5.0  | 2026-06-03     | SPU 五态、类目树、共享包优化                                                             | 已完成   |
| v0.6.0  | 2026-06-03     | SPU 扩展 goods 主表属性、品牌                                                            | 已完成   |
| v0.6.1  | 2026-06-03     | 管理端商品列表能力完善                                                                   | 已完成   |
| v0.7.0  | 2026-06-03     | 商品表单增强（富文本/上传/标签/模板）                                                    | 已完成   |
| v0.8.0  | 2026-06-03     | 标签/首页/搜索 + 商品详情页改造 + 服务保障                                               | 已完成   |
| v0.9.0  | 2026-06-03     | Admin 商城用户管理 + API URI 版本 v2                                                     | 已完成   |
| v0.9.1  | 2026-06-03     | Monorepo 编译配置收敛（shared/API/前端）                                                 | 已完成   |
| v0.10.0 | 2026-06-04～05 | TCB 全量部署（Prisma 云托管 + /web·/admin 静态路径与同域登录隔离）                       | 已完成   |
| v0.10.1 | 2026-06-05     | 商品卡片售价与仪表盘统计修复                                                             | 已完成   |
| v0.11.0 | 2026-06-05     | Harness 工程化体系（约束/验证/Spec/CI）                                                  | 已完成   |
| v0.12.0 | 2026-06-05     | AI 对话助手（浮动按钮/对话弹窗/Function Calling）                                        | 已完成   |
| v0.12.1 | 2026-06-05     | 模拟支付流水号修复、AI 浮层层级调整                                                      | 已完成   |
| v0.12.2 | 2026-06-05     | 订单信息与确认订单体验（详情 / 结算商品·地址·图预览 / ImagePreviewCarousel / cart 扩展） | 当前基线 |

---

## v0.1.0 — 架构与设计文档（2026-06-03）

### 交付内容

- 《轻量电商系统架构说明书》：领域模型、订单/库存/支付状态机、技术选型（NestJS + Nuxt + React Admin）。
- 多端开发设计文档：服务端、Web 商城、管理后台、共享接口与约定。
- `docs/README.md` 文档索引与变更同步规则。

### 说明

本阶段以 **文档驱动** 为主，确立三端分工与 API 契约，尚未落地完整代码。

---

## v0.2.0 — Monorepo 骨架与 dev.sh（2026-06-03）

### 交付内容

| 模块     | 内容                                                      |
| -------- | --------------------------------------------------------- |
| 工程结构 | `apps/api`、`apps/web`、`apps/admin`、`packages/shared`   |
| 基础设施 | `docker-compose.yml`（MySQL 8、Redis 7）                  |
| 共享包   | `@simplemall/shared` 枚举与 DTO 约定（CJS + ESM 双构建）  |
| 开发脚本 | `dev.sh` 一键启停 API / Web / Admin，日志目录 `.dev/logs` |
| API 基础 | Prisma 初版迁移、健康检查、JWT 管理端/用户端鉴权骨架      |

### 升级注意

- 须安装 Docker Desktop，默认数据库账号 `root:root`，库名 `simplemall`。
- 根目录 `pnpm-workspace.yaml` 不可删，Monorepo 依赖通过 workspace 解析。

---

## v0.3.0 — 三端核心业务闭环（2026-06-03）

### API（`apps/api`）

- **用户端**：注册/登录、类目树、SPU 列表/详情、购物车、地址、下单、模拟支付、订单列表/详情/取消/确认收货/退货申请。
- **管理端**：类目 CRUD、SPU/SKU 矩阵创建与编辑、上下架、SKU 库存调整、订单列表/详情/发货/退货审核、SUPER 模拟支付回调。
- **领域**：库存预占与释放、订单超时取消、已发货自动完成（定时任务）、`UserTypeGuard` / `AdminTypeGuard`。

### Web 商城（`apps/web`）

- 首页、分类、商品列表/详情（SKU 选择）、购物车、结算、订单、用户地址、登录/注册。

### 管理后台（`apps/admin`）

- 登录、仪表盘、类目、商品、订单、模拟支付（SUPER）。

### 数据

- Prisma `seed`：管理员 `admin/admin123`、用户 `13800138000/user123`、演示 SPU/SKU。

---

## v0.4.0 — 演示数据、工程修复与后台体验（2026-06-03）

### 数据与脚本

- **Seed 扩充**：9 个类目（含二级）、12 个 SPU、24 个 SKU，覆盖服装/数码/家居/食品。
- `dev.sh` 启动流程增加 `prisma seed`；修复 `ROOT` 未绑定变量等问题。
- 管理端登录 401：JWT 配置、`seed` 与 HTTP 错误提示优化。

### Web（Nuxt）开发环境

- macOS `vite-node` Unix socket 路径过长：Web 使用 `TMPDIR=/tmp`（`apps/web/scripts/dev.sh`）。
- `#app-manifest` 预转换失败：关闭 `experimental.appManifest`，避免误删 `.nuxt` 的 `predev`。
- `dev.sh` 修复 `exec TMPDIR=...` 导致启动失败的问题。

### 管理后台 UI（v0.4）

- 深色侧栏、面包屑、统一页头与卡片布局。
- 登录页分栏品牌区；仪表盘统计与快捷入口。
- 商品列表缩略图、价格格式化、订单状态彩色标签。

---

## v0.5.0 — SPU 五态、类目树、共享包（当前基线，2026-06-03）

### SPU 商品状态（Breaking：枚举扩展）

| 状态值 `SpuStatus` | 中文   | C 端列表 | 可购买       |
| ------------------ | ------ | -------- | ------------ |
| `NOT_LISTED`       | 未上架 | 否       | 否           |
| `ON_SALE`          | 已上架 | 是       | 是（有库存） |
| `SOLD_OUT`         | 已售罄 | 是       | 否           |
| `OFF_SALE`         | 已下架 | 否       | 否           |
| `RESTOCKING`       | 补货中 | 是       | 否           |

- 原 `DRAFT` 废弃，等同 `NOT_LISTED`；seed 会自动迁移历史数据。
- 库存变更后自动同步：已上架 ↔ 已售罄；补货中有库存 → 已上架。
- 实现位置：`packages/shared/src/spu-status.ts`，文档见《共享接口与约定》。

### 管理后台

- 商品列表：**状态列**、状态筛选、行内切换。
- **类目管理**：由扁平表格改为**树形表格**，支持「新增一级类目」「新增子类目」。
- `@simplemall/shared`：开发态直连源码，避免 Vite 预构建缓存缺少新导出（如 `SPU_STATUS_LABEL`）。

### Web 商城

- 商品列表/详情展示售罄、补货中标签；不可购时禁用加购并提示。

### 文档同步

- 架构说明书 v1.1、共享约定、三端开发设计文档、`docs/README.md` 变更规则均已更新。

### 升级注意

```bash
pnpm --filter @simplemall/shared build
cd apps/api && pnpm prisma:seed
./dev.sh stop && ./dev.sh
# Admin 若报 shared 导出错误：
cd apps/admin && rm -rf node_modules/.vite && pnpm dev
```

---

## v0.6.0 — 商品属性扩展（对齐 goods 主表，2026-06-03）

### 交付内容

- **数据库**：`spus` 增加货号、短标题、副标题、品牌、三级类目路径、轮播/参数/标签、价格与物流、营销标、排序、上架时间等；新增 `brands` 表。
- **API**：管理端创建/编辑/详情返回扩展字段；`GET/POST /admin/brands`；库存同步时更新 `totalStock`。
- **Admin**：商品表单分栏（基础/图片/价格/物流/营销）；列表展示货号、库存、销量、排序。
- **Web**：列表/详情透出副标题、市场价、标签等（C 端可读字段）。
- **文档**：《共享接口与约定》goods ↔ spus 字段对照表。

### 升级注意

```bash
cd apps/api && pnpm prisma migrate deploy
pnpm prisma generate
pnpm prisma:seed
pnpm --filter @simplemall/shared build   # 若仅改 API 可跳过
./dev.sh stop && ./dev.sh
```

---

## v0.6.1 — 管理端商品列表完善（2026-06-03）

### 交付内容

- **API**：`GET /admin/spus` 支持 `page`、`pageSize`、`status`、`categoryId`、`brandId`、`keyword` 筛选，响应 `{ list, total, page, pageSize }`。
- **Admin 商品列表**：关键词/状态/类目/品牌筛选与重置；服务端分页；行展开 SKU 明细表；售价区间、营销标签、低库存高亮；货号复制；C 端预览；快捷上架/下架与更多状态；库存支持「设为」与「增减」两种调整方式。

### 升级注意

拉取代码后重启 API 与 Admin 即可，无数据库迁移。可选环境变量 `VITE_STORE_URL`（默认 `http://localhost:3000`）用于列表「预览」跳转。

---

## v0.7.0 — 商品表单增强（2026-06-03）

### 交付内容

- **数据库**：`tags` 词库表、`express_templates` 运费模板表；演示 seed 含模板与常用标签。
- **API**：`POST /admin/upload` 图片上传；`GET/POST /admin/tags`；品牌 PUT/DELETE；运费模板 CRUD。
- **Admin 商品表单**：详情富文本（react-quill）；主图/轮播图上传；标签检索与回车创建；规格维度可删（至少保留 1 个）；品牌/运费模板下拉联动管理页。
- **Admin 菜单**：品牌管理、运费模板。

### 升级注意

```bash
cd apps/api && pnpm exec prisma migrate deploy && pnpm prisma:seed
pnpm install   # admin 新增 react-quill
./dev.sh stop && ./dev.sh
```

上传文件目录 `apps/api/uploads/`（已 gitignore），静态访问路径 `/api/v1/uploads/*`。

---

## v0.8.0 — 标签管理、商城首页与商品详情页（2026-06-03）

### 标签与首页 / 搜索

- **标签表**：`status`（启用/停用）、`isHot`（热门）；Admin 标签管理 CRUD；商品表单仅可选启用标签。
- **API**：`GET /home`、`GET /tags`、`GET /tags/hot`；`GET /spus` 支持 `tag`、`recommend`、`keyword`、统一搜索参数 **`q`**（启用标签名按标签筛，否则按关键词）。
- **Web 顶栏（参考闲鱼）**：仅**首页**展示热门标签；点击标签跳转 **`/search?q=`**，标签无选中回显；搜索框提交同路径。
- **Web 页面**：`pages/search/index.vue` 搜索结果页；Footer；首页热门活动链至搜索页、推荐商品、「猜你喜欢」+ 类目/标签 Tab。
- **样式约定**：`.cursor/rules/web-tailwind.mdc`（Web 强制 Tailwind，非必要不新增 CSS 文件）；`web-responsive.mdc` 与之对齐。

### 商品详情页改造（`pages/products/[id].vue`）

| 模块         | 说明                                                                                               |
| ------------ | -------------------------------------------------------------------------------------------------- |
| **左右布局** | 左侧：图集轮播 + 商品详情富文本（自上而下滚动）；右侧：购买信息区，约占宽 **40%**，`sticky` 吸顶   |
| **图集**     | 组件 `ProductImageCarousel`：主图 + `images_json` 轮播，支持箭头/指示点/缩略图                     |
| **右侧信息** | 标题/状态/卖点、现价/划线价/优惠金额、配送（运费模板 + 发货地→默认地址）、服务保障标签、规格、数量 |
| **底栏操作** | 固定于右侧栏底部（≤1024px 为全屏底栏）：胶囊按钮（左渐变 + 购物车图标 / 右「立即购买」）+ 收藏     |
| **API 扩展** | `GET /spus/:id` 增加 `express`、`shipFrom`、`serviceGuarantees`（见下）                            |
| **规格交互** | 商品不可购（非已上架、全 SKU 无库存等）时规格按钮置灰；可购时无库存规格组合单独置灰                |
| **样式实现** | 详情页样式全部由 **Tailwind**（`bp-*` 断点）承担，已移除 `assets/css/product-detail.css`           |

### 服务保障

- **数据库**：`service_guarantees` 表；`spus.service_list`（逗号分隔名称，与 `tag_list` 类似）。
- **API**：`GET/POST/PUT/DELETE /admin/service-guarantees`；商品保存校验均为启用项；详情返回 `serviceGuarantees[]`。
- **Admin**：侧栏「服务保障」CRUD；商品表单多选 `ServiceGuaranteeInput`。
- **Seed**：默认「破损包退、假一赔十、退货宝、极速退款」并写入演示商品。

### 其它 Web

- 顶栏/布局样式文件：`header.css`、`layout.css`（首页 `home-content` 最小宽度等）仍保留；详情页不再单独维护 CSS 文件。

### 升级注意

```bash
cd apps/api && pnpm exec prisma migrate deploy && pnpm exec prisma generate && pnpm prisma:seed
pnpm --filter @simplemall/shared build
./dev.sh stop && ./dev.sh
```

迁移包括：`20260603150000_tag_status_hot`（若未执行）、`20260603160000_service_guarantees`（`service_list` 字段）。已部署库仅需 `migrate deploy` + `seed`（seed 会 upsert 服务保障词库）。

---

## v0.9.0 — Admin 商城用户管理 + API v2（2026-06-03）

### API

- **URI 版本**：全局前缀 `/api`，默认 `v1`；既有接口路径不变（如 `/api/v1/health`）。
- **v2 模块**：`GET/PUT /api/v2/admin/users`、`GET /api/v2/admin/users/:id`（`AdminTypeGuard`）。
- 实现：`apps/api/src/modules/admin/user/`（`AdminUserService` + `AdminUserController`）。

### 管理后台

- 侧栏「商城用户」、路由 `/users`；`httpV2` 客户端默认 `baseURL` `/api/v2`（可配 `VITE_API_V2_BASE`）。
- 列表：分页、手机号/昵称搜索；弹窗编辑昵称。

### 升级注意

- 其余 Admin 接口仍走 `http.ts` → `/api/v1`；仅用户管理走 v2。
- 开发代理已转发 `/api`，无需额外配置。

---

## v0.9.1 — 编译配置收敛（2026-06-03）

### 交付内容

| 模块                      | 变更                                                                                                                                      |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/shared`         | 删除 `tsconfig.base/cjs/esm` 与 CJS+ESM 双产物；单一 `tsconfig.json` → `dist/`（CommonJS，供 API 运行时）                                 |
| `apps/api`                | 合并 `tsconfig.build.json` 入 `tsconfig.json`；`dev` 改为 `nest start --watch`（不再每次全量 build）；`nest-cli` 使用默认 `tsconfig.json` |
| `apps/web` / `apps/admin` | Vite/Nuxt 统一 alias 到 `packages/shared/src`，由 bundler 编译，避免 CJS dist 与 Rollup 命名导出不兼容                                    |
| 根目录                    | `pnpm build` 改为 `pnpm -r build`（按依赖拓扑，shared 先于 api）                                                                          |
| 工程                      | `.gitignore` 忽略 `packages/shared/src` 下误生成的 `.js/.d.ts/.map`                                                                       |

### 升级注意

- 仅改 API 时仍需 `predev` / `dev.sh` 先编译 shared（Nest 运行时读 `dist`）；Web/Admin 开发可不单独 `shared build`。
- 已删除 `tsconfig.build.json`、`packages/shared/tsconfig.*.json`（除根 `tsconfig.json`）；本地可删 `apps/api/tsconfig.build.tsbuildinfo`。

---

## v0.10.0 — TCB 全量部署（2026-06-04～05）

### 背景

首期将 API、Web、Admin 部署至腾讯云开发：云托管跑 NestJS + Prisma + 云开发 MySQL；静态托管同域名承载商城与管理端。上线后发现 Web 在根路径 `/`、Admin 在 `/admin` 时，Admin 路由或 401 会误跳 `/login`（商城登录页），两端 token 混用；后续在同版本内补齐子路径隔离与 Admin 客户端路由修复。

### 交付内容

| 模块     | 内容                                                                                                                                                            |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 数据访问 | **沿用 Prisma + `DATABASE_URL`**，对接云开发 MySQL（控制台 ORM → Prisma 指引），**不**引入 `@cloudbase/node-sdk` `rdb()`                                        |
| 云托管   | `deploy/tcb/Dockerfile` 多阶段构建、`deploy/tcb/env.example`；`prisma generate` 引擎打入镜像，修复云托管未初始化崩溃                                            |
| API 运行 | `HOST=0.0.0.0`、`PUBLIC_API_BASE` 上传绝对 URL、`ENABLE_CRON` 多副本开关；`PrismaService` 在 `DATABASE_URL` 无效时跳过 `$connect`，保证 `/api/v1/health` 可探活 |
| 静态路径 | Web → `/web`（`NUXT_APP_BASE_URL=/web/`）；Admin → `/admin`（`VITE_BASE=/admin/`）；根 `/` 跳转 `/web/`                                                         |
| 部署脚本 | `build-static.sh`、`deploy-static.sh`（`pnpm run deploy:tcb:static`）、`cleanup-legacy-web.sh`、`hosting-routing-rules.example.json`                            |
| 管理端   | `BrowserRouter` `basename`；`handleAdminUnauthorized` 跳转 `/admin/login`，不再误跳商城 `/login`                                                                |
| 工程     | `cloudbaserc.json`、根及 `deploy/tcb` `.dockerignore`（排除 `**/.env`）                                                                                         |
| 文档     | `docs/TCB部署说明.md`、`.env.tcb.example`                                                                                                                       |

### 涉及模块

`deploy/tcb`、`cloudbaserc.json`、`apps/api`（main、upload、app.module、nest-cli、prisma.service）、`apps/admin`（vite base、basename、401 跳转）、`package.json`（根）、`Dockerfile`、`.dockerignore`、`docs`

### 升级注意

**云托管 API**

1. 云开发控制台创建 MySQL，复制 **内网** 连接串到 `DATABASE_URL`，另配 `REDIS_URL` 等（见 `deploy/tcb/env.example`）后重新部署。
2. 配置数据库后执行 `prisma migrate deploy`。
3. 根目录 `.env` 配置 `CLOUDBASE_ENV_ID`（勿提交），`tcb framework deploy` 或 `pnpm run build:tcb:api`。
4. CLI 灰度提示可回答 `No` 或使用 `printf 'n\n' | tcb cloudrun deploy ... --force`。

**静态托管 Web / Admin**

5. 设置 `NUXT_PUBLIC_API_BASE`、`VITE_API_BASE`、`VITE_API_V2_BASE` 后执行 `pnpm run deploy:tcb:static`（上传 `/web`、`/admin`、根 `index.html`）。
6. 从旧根路径 Web 迁移后运行 `cleanup-legacy-web.sh --yes`，删除根下 `login/`、`products/`、`_nuxt/` 等遗留。
7. 配置 SPA 回退（`/web/*` → `web/200.html`，`/admin/*` → `admin/index.html`）；`CORS_ORIGINS` 同域填一个静态域名即可。

**其他**

8. 容器内 `uploads/` 非持久化，生产图片建议后续接 COS。
9. 本地开发未设 `NUXT_APP_BASE_URL` / `VITE_BASE` 时行为不变。

---

## v0.10.1 — 商品售价与仪表盘统计修复（2026-06-05）

### 交付内容

- **API**：`mapSpuCard` 的 `minPrice` 不再向 `Math.min` 传入 `0`，修复列表卡片售价恒为 ¥0.00。
- **管理端**：仪表盘按分页接口的 `total` 汇总商品/在售/订单数，不再把 `{ list, total }` 当数组导致统计全为 0。

### 涉及模块

`api`、`admin`

### 升级注意

- 售价修复需**重新部署云托管 API**；仪表盘修复重新构建上传 Admin 静态资源即可。
- 静态构建须在 `.env` 配置 `NUXT_PUBLIC_API_BASE` 等云托管地址，见 `.env.tcb.example` 与 `deploy/tcb/resolve-api-base.sh`。

---

## v0.11.0 — Harness 工程化体系（2026-06-05）

### 交付内容

- **约束层**：扩展 `AGENTS.md`；新增 `.cursor/rules/harness-core.mdc`、`api-module.mdc`、`admin-patterns.mdc`。
- **上下文层**：`harness/progress.md` 跨会话交接；`specs/` 目录与 Spec 模板；`.agents/skills/simplemall/` 项目技能（验证、Spec、TCB）。
- **验证层**：`harness/verify.sh`（types/build/all）；`pnpm verify*` 脚本；`.cursor/hooks.json`（会话注入、git 保护、交付提醒）；GitHub Actions `harness-verify.yml`（`checkout/setup-node/pnpm` v6、Node.js 24、`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`）。
- **文档**：`docs/Harness工程化体系.md`；更新 `docs/README.md` 索引。

### 涉及模块

`docs`、`.cursor/`、`.agents/skills/simplemall/`、`harness/`、`specs/`、`.github/workflows/`、根 `package.json`

### 升级注意

- Agent 交付代码前运行：`pnpm verify:types`（大改 `pnpm verify`）。
- Cursor 用户需在 **Hooks** 设置中确认项目 hooks 已加载（或重启 Cursor）。
- CI 需仓库启用 GitHub Actions；使用 Node.js 24 与 `checkout/setup-node/pnpm` v6 Actions（消除 Node 20 弃用警告）。

---

## v0.12.0 — AI 对话助手（2026-06-05）

### 交付内容

- **Web 端**：全站浮动可拖拽 AI 按钮（`AiChatButton`）；悬浮对话弹窗（`AiChatDialog`）支持消息气泡、快捷提示、流式回复、Markdown 渲染；`useChat` composable 封装 SSE 流式+非流式回退；`stores/chat.ts` Pinia 状态管理；未登录/401 时引导登录（弹窗 + 提示气泡）。
- **API 端**：`POST /api/v1/chat/completions`（流式 SSE + 非流式，需登录）；`ChatService` 整合 DeepSeek V4 Function Calling（`callLlm → executeTool → callLlm` 循环），支持 8 个工具；`orderNo`（如 `SMxxx`）与数字 ID 均可解析；对话历史 Redis 存储（30 分钟 TTL，最近 10 轮）。
- **配置**：通过 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` 环境变量配置大模型。

### 涉及模块

`apps/web`（组件/组合式函数/Store/布局）、`apps/api`（chat 模块/控制器/DTO/工具定义）、`.env.example`

### 升级注意

- 需在 `apps/api/.env`（及 TCB 云托管环境变量）配置 `LLM_API_KEY`（DeepSeek 或其他 OpenAI 兼容 API Key），否则 AI 服务不可用。
- `LLM_BASE_URL` 默认 `https://api.deepseek.com/v1`，`LLM_MODEL` 默认 `deepseek-chat`。
- TCB 生产：`simplemall-api-009` + 静态 `/web`·`/admin` 已部署（2026-06-05）。
- 优惠券工具（`get_coupons`）返回「暂未上线」，优惠券功能本身尚未实现。
- 部署前运行 `pnpm verify` 确认构建通过。

---

## v0.12.1 — 模拟支付与 AI 浮层修复（2026-06-05）

### 交付内容

- **API**：`PaymentService.create` 修复同一订单重复发起支付时 `paymentNo` 与 `mock-pay` URL 不一致导致「支付单不存在」；INIT 状态复用原流水号，CLOSED 后重新支付写入新流水号。
- **Web**：AI 入口/面板 z-index 调整为 90/95（低于登录弹窗 100）；面板打开时隐藏入口按钮，避免重叠。

### 涉及模块

`apps/api`（payment.service）、`apps/web`（AiChatButton、AiChatDialog、layout.css）

### 升级注意

- 修复后需重启 API；历史无效 `mock-pay` 链接需从订单页重新发起支付。
- 无数据库迁移。

---

## v0.12.2 — 订单信息与确认订单体验（2026-06-05）

> **当前基线**。订单详情「订单信息 / 商品信息」与确认订单页一次性交付，开发过程分批落地，下文为最终形态总结。

### 订单详情页（`/orders/[id]`）

#### 订单信息

| 展示项   | 说明                                                 |
| -------- | ---------------------------------------------------- |
| 配送地址 | 下单快照 `addressJson`：收货人、手机、省市区详细地址 |
| 下单时间 | `createdAt`                                          |
| 更新时间 | `updatedAt`（与下单时间不同时展示）                  |
| 支付方式 | `payment.channel`（支付宝 / 微信）                   |
| 支付时间 | `payment.paidAt`                                     |
| 买家留言 | `remark`                                             |
| 物流信息 | `logisticsJson`：物流公司、快递单号（有物流时展示）  |

- **布局**：key-value 左右结构——标签居左、内容居右；多行字段（地址、留言）右对齐换行；物流并入「订单信息」卡片，不再单独成块。

#### 商品信息

与确认订单页视觉对齐，包含：

- 主图可点击全屏预览；多图角标 + `ImagePreviewCarousel` 左右轮播
- 标题、规格、服务保障橙色标签、售价与划线原价（`spuMarketPrice`）
- 商品小计、运费、优惠、实付汇总

数据来源：既有订单详情 API（`addressJson`、订单项快照字段），无后端结构变更。

---

### 确认订单页（`/checkout`）

#### 收货地址

- 地址卡片整块可点击 → `/user/addresses?pick=1&from=...`
- 选中后 `sessionStorage` 回显；`pick=1` 模式用 `router.back()` 返回，避免后退历史循环
- 地址管理页新增/编辑弹窗支持右上角关闭并重置表单

#### 商品信息

| 展示项          | 购物车结算                                 | 立即购买                    |
| --------------- | ------------------------------------------ | --------------------------- |
| 主图 + 多图预览 | `/cart` → `sku.mainImage`、`sku.spuImages` | URL：`image`、`images`      |
| 标题 / 规格     | `sku.spuTitle`、`sku.specs`                | URL：`title`、`specs`       |
| 服务保障        | `sku.serviceGuarantees`                    | URL：`services`（JSON）     |
| 售价 / 划线原价 | `sku.price`、`sku.spuMarketPrice`          | URL：`price`、`marketPrice` |
| 数量            | 购物车选中行 `quantity`                    | URL：`qty`                  |

- 商品行样式对齐订单详情：图片、规格、服务保障标签、售价 + 划线原价
- 图片预览复用 `ImagePreviewCarousel`

#### 金额与提交

- 商品合计、应付金额；提交订单并跳转模拟支付（逻辑未变）

---

### API 与立即购买参数（`apps/api` + `products/[id].vue`）

**购物车 `/cart` 条目 `sku` 扩展字段**：

| 字段                | 来源                         | 用途           |
| ------------------- | ---------------------------- | -------------- |
| `spuImages`         | SPU `imagesJson`             | 结算页多图预览 |
| `spuMarketPrice`    | SPU `marketPrice`            | 划线原价       |
| `serviceGuarantees` | SPU `serviceList` 解析启用项 | 服务保障标签   |

**立即购买 query 参数**（商品详情 `buyNow`）：

`skuId`、`qty`、`buyNow=1`、`title`、`price`、`image`、`specs`、`images`、`marketPrice`、`services`

---

### 涉及模块

| 模块 | 文件                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Web  | `pages/orders/[id].vue`、`pages/checkout.vue`、`pages/user/addresses.vue`、`pages/products/[id].vue`、`components/ImagePreviewCarousel.vue` |
| API  | `modules/core/cart/cart.service.ts`                                                                                                         |

### 升级注意

- 购物车响应新增字段，旧客户端可忽略；**需重启 API**，无数据库迁移。
- 订单详情为纯前端展示增强，复用现有订单详情接口。
- 全屏预览与商品详情页内嵌 `ProductImageCarousel` 职责分离。

---

## 后续规划（未实现）

以下为架构文档中的扩展方向，**尚未排期开发**：

| 方向       | 说明                                                        |
| ---------- | ----------------------------------------------------------- |
| 支付对接   | 真实支付宝/微信回调                                         |
| 权限细化   | 管理端 OPERATOR 角色菜单隔离                                |
| 搜索与排序 | 商品 ES/全文检索、销量排序（列表已支持关键词筛选）          |
| 营销       | 优惠券、满减、秒杀                                          |
| 部署       | TCB 已支持；Harness CI 已接入类型检查；COS 上传、E2E 待完善 |

---

## 文档与代码版本对应

| 文档                        | 最近修订 |
| --------------------------- | -------- |
| 轻量电商系统架构说明书      | v1.1     |
| 共享接口与约定              | SPU 五态 |
| 服务端 / Web / 管理后台设计 | 随 v0.5  |
| Harness 工程化体系          | v1.0     |
| 本文档（版本迭代说明）      | v0.12.2  |

**维护约定**：每次发版或合并重要特性后，在本文件顶部「版本总览」增一行，并在对应章节补充交付内容与升级注意点；若涉及接口/枚举变更，同步更新《共享接口与约定》。

---

## 修订记录

| 版本 | 日期       | 说明                                                                |
| ---- | ---------- | ------------------------------------------------------------------- |
| 1.0  | 2026-06-03 | 初版：v0.1.0～v0.5.0 迭代说明                                       |
| 1.1  | 2026-06-03 | v0.8.0 补充商品详情页、搜索、服务保障                               |
| 1.2  | 2026-06-03 | v0.9.0 商城用户管理、API v2 约定                                    |
| 1.3  | 2026-06-03 | v0.9.1 Monorepo 编译配置收敛                                        |
| 1.4  | 2026-06-05 | v0.10.0 TCB 全量部署（云托管 + /web·/admin 静态路径与同域登录隔离） |
| 1.5  | 2026-06-05 | v0.10.1 商品 minPrice 与仪表盘 total 统计修复                       |
| 1.6  | 2026-06-05 | v0.11.0 Harness 工程化体系                                          |
| 1.7  | 2026-06-05 | v0.12.0 AI 对话助手（浮动按钮/对话弹窗/Function Calling）           |
| 1.8  | 2026-06-05 | v0.12.1 模拟支付流水号修复、AI 浮层层级调整                         |
| 1.9  | 2026-06-05 | v0.12.2 订单信息与确认订单体验（合并 v0.12.2～v0.12.6 为一条）      |
